for (let i = 0; i < 4; i++) {
        //     let adjx = x + dx[i];
        //     let adjy = y + dy[i];

        //     if (isValid(visited, adjx, adjy, ROW, COL)) {
        //         queue.push([adjx, adjy]);
        //         visited[adjx][adjy] = true;
        //     }
        // }